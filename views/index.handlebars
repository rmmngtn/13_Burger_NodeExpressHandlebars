<div class="header">
  <div class="jumbotron" style="padding: 30px; background-color: black;">
    <h1 style="text-align: center; padding: 10px; color: #ffc107; font-family: Shrikhand;"> <i
        class="fas fa-hamburger"></i>
      <br>Burgers!
    </h1>
  </div>
</div>

<main class="container">
  <div class="row">
    <div class="col-md-6" style="float: left; text-align:center; ">
      <h3>Burgers that are not yet devoured!</h3>

      <ul style="list-style-type:none;">
        {{#each burgers}}
        {{#unless devoured}}
        {{> burgers/burgers-block devoured=true}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="col-md-6" style="float:left; text-align:center; ">
      <h3>Burgers that are devoured!</h3>

      <ul style="list-style-type:none;">
        {{#each burgers}}
        {{#if devoured}}
        {{> burgers/burgers-block devoured=false}}
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>

  <br>
  <div class="row" style="text-align:center;">
    <h2>Not seeing what you want?</h2>
    <h3>Add a burger</h3>
    <form id="create-form">

      <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="name">
      </div>

      <div class="form-group">
        <label for="devoured">Devoured?</label><br>
        <input type="radio" name="devoured" value="1" id="devoured" checked> Devoured!<br>
        <input type="radio" name="devoured" value="0" id="notdevoured"> Yet to try!
      </div>

      <button type="submit">Add Burger</button>

    </form>
  </div>
</main>


<footer class="footer">
  <div class="container">
    <span class="text-muted">Burger Logger by <a href="github.com/rmmngtn">Remmington</a> </span>
  </div>
</footer>